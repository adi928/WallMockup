apiVersion: v1
kind: Service
metadata:
  name: wall-mockup
  labels:
    app: wall-mockup
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: wall-mockup
